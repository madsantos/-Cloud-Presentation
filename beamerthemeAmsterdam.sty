\DeclareOptionBeamer{compress}{\beamer@compresstrue}
\ProcessOptionsBeamer

% \newcommand{\headertext}{
%   Center for\\
%   Bioinformatics\\
% }
% 
% \newcommand{\addresstext}{
%   University of Hamburg\\
%   Center for Bioinformatics\\
%   Bundesstra\ss e 43\\
%   20146 Hamburg, Germany\\[2mm]
%   http://www.zbh.uni-hamburg.de\\
%   info@zbh.uni-hamburg.de
% }

% -- Comentar esta linha para recolocar o número total de slides -- %
\def\tirarTotalDeSlides{}

% -- Comentar esta linha para recolocar os círculos das subsesções -- %
\def\tirarCirculos{}

\newcommand{\frameofframes}{/}
\newcommand{\setframeofframes}[1]{\renewcommand{\frameofframes}{#1}}
\newcommand{\evento}{}

\setframeofframes{of}
\makeatletter
\setbeamertemplate{footline}
  {%
    \begin{beamercolorbox}[colsep=1.5pt]{upper separation line foot}
    \end{beamercolorbox}
    \begin{beamercolorbox}[ht=2.5ex,dp=1.125ex,%
      leftskip=.3cm,rightskip=.3cm plus1fil]{author in head/foot}%
      \leavevmode{\usebeamerfont{title in head/foot}\insertshorttitle}%
      %\hfill%
      %{\usebeamerfont{institute in head/foot}\usebeamercolor[fg]{institute in head/foot}\insertshortinstitute}%
    \end{beamercolorbox}%
    \begin{beamercolorbox}[ht=2.5ex,dp=1.125ex,%
      leftskip=.3cm,rightskip=.3cm plus1fil]{title in head/foot}%
      %{\usebeamerfont{title in head/foot}\insertshorttitle}%
	 
      % A mudança no template foi efetuada aqui, que é onde é removida a quantidade total de slides caso deseje-se fazer isto.
      \ifdefined \tirarTotalDeSlides
      	{\usebeamerfont{title in head/foot}\evento}%
      	\hfill%
      	{\usebeamerfont{frame number}\usebeamercolor[fg]{frame number}\insertframenumber}
      \else
      	{\usebeamerfont{title in head/foot}\evento}%
      	\hfill%
      	{\usebeamerfont{frame number}\usebeamercolor[fg]{frame number}\insertframenumber~\frameofframes~\inserttotalframenumber}
      \fi
    \end{beamercolorbox}%
    \begin{beamercolorbox}[colsep=1.5pt]{lower separation line foot}
    \end{beamercolorbox}
  }
\makeatother

\mode<presentation>

%\useoutertheme[footline=authortitle]{miniframes}
\useoutertheme[]{miniframes}
\useinnertheme{circles}
\usecolortheme{whale}
\usecolortheme{orchid}

\definecolor{beamer@blendedblue}{rgb}{0.137,0.466,0.741}

\setbeamercolor{structure}{fg=beamer@blendedblue}
\setbeamercolor{titlelike}{parent=structure}
\setbeamercolor{frametitle}{fg=black}
\setbeamercolor{title}{fg=black}
\setbeamercolor{item}{fg=black}

% Remove os círculos das subsections, que podem facilmente quebrar o template, trocando por uma barra simples que indica qual a subseção atual.
\ifdefined \tirarCirculos
\setbeamertemplate{navigation symbols}{}
\setbeamercovered{transparent=20}
\newcommand*\oldmacro{}%
\let\oldmacro\insertshorttitle%
\renewcommand*\insertshorttitle{%
  \oldmacro\hfill%
  }

\setbeamertemplate{headline}
 {%
  \begin{beamercolorbox}{section in head/foot}
  \insertsectionnavigationhorizontal{\textwidth}{}{}
  \end{beamercolorbox}%
  \begin{beamercolorbox}[ht=2.5ex,dp=1.125ex,%
    leftskip=.3cm,rightskip=.3cm plus1fil]{subsection in head/foot}
    \usebeamerfont{subsection in head/foot}\insertsubsectionhead
  \end{beamercolorbox}%
}
\fi

\mode
<all>
